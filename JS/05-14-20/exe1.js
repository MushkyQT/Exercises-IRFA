let length = parseInt(prompt("Enter length of rectangle: "));
let width = parseInt(prompt("Enter width of rectangle: "));
alert("Area is:" + length * width);
// Or document.write() instead of alert()
alert("Perimeter is:" + 2 * (length + width));